---
id: "002"
title: "Three.js bootstrap: renderer, camera, scene, ground plane"
type: code
mode: afk
status: closed
branch: tower-defense-3d-ui-prototype-run-v2-002
dependsOn:
  - "001"
acceptanceCriteria:
  - "`three` (latest stable) is added to dependencies; a new
    `src/render/three/scene.ts` (or similar) creates a `WebGLRenderer`,
    `PerspectiveCamera` positioned roughly at `(CANVAS_WIDTH/2, 200,
    CANVAS_HEIGHT + 400)` looking at `(CANVAS_WIDTH/2, 0, CANVAS_HEIGHT/2)`, and
    a `Scene` with a visible ground plane mesh."
  - Renderer mounts into the existing canvas/container in `index.html` and
    starts a render loop driven from the existing game loop tick.
  - No assets, no enemies, no towers, no gameplay wiring yet — just an empty
    scene with the ground plane visible.
  - "`bun run typecheck`, `bun run lint`, `bun run test:unit` are green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Stand up the minimal Three.js renderer behind the existing game loop. No gameplay wiring, no Kenney assets — just camera + scene + ground plane visible in the dev server.
