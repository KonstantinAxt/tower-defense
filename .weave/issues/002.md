---
id: "002"
title: Level path, Kenney asset loader, and static scene render
type: code
mode: afk
status: open
branch: weave/stretch-tower-defense-side-view-kenney/002
dependsOn:
  - "001"
acceptanceCriteria:
  - "`src/level.ts` exports a hardcoded polyline path and an array of fixed
    tower build slots"
  - Asset loader preloads required Kenney sprites from `assets/kenney/` and
    exposes them by key
  - Canvas renders background, path, and build-slot markers using loaded sprites
---

Define the single level: a hardcoded polyline waypoint array plus fixed ground build-slot coordinates. Add a simple image loader that preloads Kenney sprites (towers, enemies, tiles, projectiles) and resolves before the loop starts. Render the static scene: background tiles, path visualization, and empty build slots.
