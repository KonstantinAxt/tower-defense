---
id: "011"
title: PLAYTEST rubric + per-variant screenshots
type: write
mode: afk
status: merge-conflict
branch: tower-defense-3d-ui-prototype-run-v2-011
dependsOn:
  - "010"
acceptanceCriteria:
  - "`PLAYTEST.md` is updated with a 3-axis 1–5 rubric (clarity, perf, delight)
    per variant A/B/C, plus a section for the human's notes; existing manual
    playtest content is preserved or merged sensibly."
  - A Playwright (or scripted) step captures one mid-wave screenshot per variant
    and writes them to a known location (e.g.
    `test-results/variant-{a,b,c}.png`), runnable via an existing or new `bun
    run` script.
  - Generated screenshots are referenced from `PLAYTEST.md` so a reviewer can
    open the doc and see all three side by side.
  - "`bun run typecheck`, `bun run lint`, `bun run test:unit`, `bun run
    test:e2e` are green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Final slice. Produces the human-facing eval artifact: the rubric and three screenshots. The 60-fps perf check and rubric scoring themselves are completed by the human against this scaffolding (per the locked decisions, picking a winner is a separate follow-up run).
