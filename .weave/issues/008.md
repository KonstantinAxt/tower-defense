---
id: "008"
title: Variant B — Diegetic warm (materials, lighting, HUD, particles)
type: code
mode: afk
status: open
branch: tower-defense-3d-ui-prototype-run-v2-008
dependsOn:
  - "006"
  - "007"
acceptanceCriteria:
  - "Register a `VariantModule` `B` (diegetic warm): PBR-ish materials
    (Lambert/Standard) using Kenney vertex colors + light textures,
    brass/wood/amber `#ffb454` palette, warm directional sun + soft contact
    shadow, no postprocess."
  - HUD is a small brass plaque mounted in-scene at upper-left; build menu is a
    cogwheel that flips open from the selected slot; hit particles are smoke +
    brass shell ejection.
  - "`tokens/b-diegetic.design.md` exists with the variant's tokens and passes
    `bunx @google/design.md lint tokens/*.design.md`."
  - "`?ui=B` renders the diegetic look end-to-end and `bun run test:e2e` against
    it is green; `bun run typecheck`, `bun run lint`, `bun run test:unit` are
    green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Ship variant B as a single issue: tabletop diorama / miniature vibe. Lighting carries the look — no postprocess.
