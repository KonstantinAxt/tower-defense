---
id: "003"
title: Coord helpers + raycast click pipeline
type: code
mode: afk
status: closed
branch: tower-defense-3d-ui-prototype-run-v2-003
dependsOn:
  - "002"
acceptanceCriteria:
  - "Add `worldFromGameCoord(p: Point): THREE.Vector3` returning `(p.x, 0, p.y)`
    and `gameFromWorldHit(hit: THREE.Vector3): Point` returning `{ x: hit.x, y:
    hit.z }`, with unit tests covering round-trip and edge values."
  - Canvas click handler raycasts against the ground-plane mesh and passes the
    resulting game-coord `Point` to the existing `pickSlotAt` (or equivalent)
    selection function.
  - Clicking on the ground in the dev server logs/selects a slot via the
    existing slot-selection logic (verifiable manually); no visual feedback
    required yet.
  - "`bun run typecheck`, `bun run lint`, `bun run test:unit` are green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Bridge between Three.js world space and the 2D gameplay coord system. Adds tested helpers and wires raycast clicks into the existing slot-selection code path.
