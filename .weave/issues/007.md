---
id: "007"
title: Variant A — Holographic (materials, lighting, postprocess, HUD, particles)
type: code
mode: afk
status: closed
branch: tower-defense-3d-ui-prototype-run-v2-007
dependsOn:
  - "006"
acceptanceCriteria:
  - "Register a `VariantModule` `A` (holographic): emissive + translucent
    materials over Kenney meshes, cyan `#5ce1ff` on near-black palette, bloom +
    scanlines + chromatic aberration via `three/examples/jsm/postprocessing/*`,
    with `prefers-reduced-motion` disabling postprocess."
  - HUD is floating cyan numerals as an orthographic overlay (or extruded text);
    build menu is a holographic 3D card that flies in ~200px from the selected
    slot; hit particles are glitch/spark.
  - "`tokens/a-holographic.design.md` exists with the variant's
    palette/material/post tokens and passes `bunx @google/design.md lint
    tokens/*.design.md`; this lint command is added to the weave feedback-loop
    commands."
  - "`?ui=A` renders the holographic look end-to-end and `bun run test:e2e`
    against it is green; `bun run typecheck`, `bun run lint`, `bun run
    test:unit` are green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Ship variant A as a single issue (within ≤4 ACs / ≤30 turns) covering materials, lighting, postprocess, HUD, build menu, particles, plus its DESIGN.md tokens. Sci-fi terminal vibe (Tron / Dead Space / Mass Effect).
