---
id: "009"
title: Variant C — Arcade pop (toon materials, outline, HUD, particles)
type: code
mode: afk
status: in-progress
branch: tower-defense-3d-ui-prototype-run-v2-009
dependsOn:
  - "006"
  - "007"
  - "008"
acceptanceCriteria:
  - "Register a `VariantModule` `C` (arcade pop): `MeshToonMaterial` over Kenney
    meshes with an outline pass (`three/examples/jsm/postprocessing/*`),
    saturated neon palette (magenta `#ff3df5`, lime `#7cff5c`, cyan `#5ce1ff`),
    flat lighting / no shadows."
  - HUD is bouncy speech-bubble counters with squash/stretch; build menu is a
    bouncy radial wheel rotating around the selected slot; hit particles are
    confetti + comic-book stars.
  - "`tokens/c-arcade.design.md` exists with the variant's tokens and passes
    `bunx @google/design.md lint tokens/*.design.md`."
  - "`?ui=C` renders the arcade look end-to-end and `bun run test:e2e` against
    it is green; `bun run typecheck`, `bun run lint`, `bun run test:unit` are
    green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Ship variant C as a single issue: cartoon battlefield / slot-machine energy. Outline pass is the only postprocess.
