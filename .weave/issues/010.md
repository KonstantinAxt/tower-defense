---
id: "010"
title: Playwright project matrix across A/B/C
type: code
mode: afk
status: closed
branch: tower-defense-3d-ui-prototype-run-v2-010
dependsOn:
  - "007"
  - "008"
  - "009"
acceptanceCriteria:
  - "`playwright.config.ts` declares three projects (`ui-a`, `ui-b`, `ui-c`)
    that each run the existing `smoke.spec.ts` and `gameplay.spec.ts` against
    `?ui=A`, `?ui=B`, `?ui=C` respectively, sharing the same dev server."
  - Specs read the variant from a Playwright project option / env var rather
    than hardcoding the URL, so the same spec file runs unchanged across all
    three projects.
  - "`bun run test:e2e` runs all three projects and is green end-to-end."
  - "`bun run typecheck`, `bun run lint`, `bun run test:unit` are green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Wire the Playwright project matrix so the same state-based specs cover all three variants in one CI command. Kept separate from the variant issues per the task's hard slicing rules.
