---
id: "006"
title: "`window.__td.testApi` + restored Playwright smoke"
type: code
mode: afk
status: in-progress
branch: tower-defense-3d-ui-prototype-run-v2-006
dependsOn:
  - "005"
acceptanceCriteria:
  - "`window.__td.testApi` exposes at minimum `clickSlot(index)` and
    `buyTower(slotIndex, towerType)` and any helpers needed by the existing
    gameplay E2E to drive the game state-based, plus a time-acceleration hook
    compatible with the existing gameplay spec."
  - "`e2e/smoke.spec.ts` and `e2e/gameplay.spec.ts` are un-skipped and updated
    to use `window.__td.testApi` instead of canvas pixel/coord clicks; both pass
    against the neutral variant."
  - "`bun run test:e2e` is green; `bun run typecheck`, `bun run lint`, `bun run
    test:unit` are green."
  - Test API is documented with a short comment block listing the exposed
    methods.
prd: tower-defense-3d-ui-prototype-run-v2
---

Restore green E2E by re-grounding the smoke and gameplay specs on a state-based test API. This closes out the foundation; variants A/B/C can now fan out in parallel.
