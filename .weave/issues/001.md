---
id: "001"
title: ECS core, fixed-timestep loop, and canvas bootstrap
type: code
mode: afk
status: open
branch: weave/stretch-tower-defense-side-view-kenney/001
dependsOn: []
acceptanceCriteria:
  - "`src/ecs/` exposes World, entity create/destroy, and component
    add/get/remove with unit tests"
  - Fixed-timestep loop (accumulator) drives systems at a stable dt; pause flag
    halts updates but keeps rendering
  - A single `<canvas>` mounts from `src/main.ts` and clears each frame; smoke
    E2E still passes
---

Implement the ECS primitives (World, entity IDs, component maps, system runner) and the fixed-timestep game loop. Wire `main.ts` to create the canvas, instantiate the World, and run empty update/render passes. Add bun unit tests for ECS add/get/remove and loop accumulator math.
