---
id: "001"
title: Remove 2D canvas renderer and obsolete render tests
type: refactor
mode: afk
status: open
branch: tower-defense-3d-ui-prototype-run-v2-001
dependsOn: []
acceptanceCriteria:
  - "`src/render/scene.ts` and any 2D-canvas-specific draw code is deleted;
    gameplay modules (`level.ts`, `enemies.ts`, `towers.ts`, `waves.ts`,
    `save.ts`, ECS) are untouched."
  - Unit tests that asserted on 2D-canvas rendering are removed; remaining `bun
    run test:unit`, `bun run lint`, `bun run typecheck` are green.
  - "`e2e/smoke.spec.ts` is marked `test.skip` (or equivalent) with a TODO
    comment referencing issue 006; gameplay E2E is similarly skipped."
  - "`index.html` still mounts and the dev server starts without runtime errors,
    even though no game visuals render."
prd: tower-defense-3d-ui-prototype-run-v2
---

Tear out the 2D top-down canvas renderer so the 3D foundation can be built without legacy code in the way. Game is allowed to be visibly broken at the end of this slice — task explicitly permits it. Smoke + gameplay E2E specs are skipped (not deleted) and will be re-enabled in 006.
