---
id: "005"
title: VariantModule interface + `?ui=` router
type: code
mode: afk
status: closed
branch: tower-defense-3d-ui-prototype-run-v2-005
dependsOn:
  - "004"
acceptanceCriteria:
  - Define a `VariantModule` interface (e.g. `{ id, applyMaterials,
    setupLighting, setupPostprocess, mountHud, mountBuildMenu, spawnParticles
    }`) covering the per-variant extension points; the neutral renderer from 004
    is refactored to implement it as `neutral`.
  - "`?ui=A|B|C|neutral` query param selects a `VariantModule` at boot; default
    is `A` when no param is present, falling back to `neutral` if `A` isn't
    registered yet."
  - Only the `neutral` module is registered at this point; loading `?ui=A`
    without A registered logs a clear warning and falls back to `neutral`.
  - "`bun run typecheck`, `bun run lint`, `bun run test:unit` are green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Introduce the variant abstraction and routing so A/B/C can be slotted in independently as parallel issues. Default `?ui=A` with `neutral` fallback keeps the dev server usable.
