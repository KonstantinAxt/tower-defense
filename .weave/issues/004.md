---
id: "004"
title: Kenney 3D asset loader + neutral variant rendering
type: code
mode: afk
status: open
branch: tower-defense-3d-ui-prototype-run-v2-004
dependsOn:
  - "003"
acceptanceCriteria:
  - "`src/assets/loader.ts` (or a sibling `loader3d.ts`) loads Kenney 3D meshes
    from `assets/kenney3d/` (assumed pre-seeded by human, mirroring
    `assets/kenney/`), with a graceful console warning if the directory is
    missing."
  - A neutral renderer adapter spawns/updates/removes Three.js meshes for
    enemies, towers, and the path polyline, driven by ECS state each tick;
    per-mesh `modelHeight` constant is used for ground placement.
  - "Running the dev server with no `?ui=` query renders a playable game in 3D
    with plain (neutral) materials: enemies walk the path, towers can be placed
    and shoot, waves advance."
  - "`bun run typecheck`, `bun run lint`, `bun run test:unit` are green."
prd: tower-defense-3d-ui-prototype-run-v2
---

Make the game visibly playable in 3D using Kenney 3D meshes with neutral materials. This is the first slice where the game is end-to-end functional again.
